name: Generate Java Sources

on:
  push:
    paths:
      - 'api-specifications/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Generate Sdk
        run: |
          chmod +x ./scripts/build_java_sdk.sh
          ./scripts/build_java_sdk.sh
        shell: bash

      - name: Upload Sdk
        uses: actions/upload-artifact@v2
        with:
          name: generated-sources-java
          path: ./generated-sources/java
      
      - name: Send success notification
        run: |
          chmod +x ./scripts/send-sdk-build-success-notification-sdk.sh
          ./scripts/send-sdk-build-success-notification-sdk.sh generate_java_sources.yml Java
      
      - name: Step failure notification
        if: ${{ failure() }}
        run: |
          chmod +x ./scripts/send-sdk-build-failure-notification.sh
          ./scripts/send-sdk-build-failure-notification.sh generate_java_sources.yml Java