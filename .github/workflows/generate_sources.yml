name: Generate Sources

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Make gradlew executable
        run: chmod +ux ./gradlew
      - uses: actions/checkout@v2
      - name: Generate Sdks
        run: |
          chmod +ux ./scripts/build_sdk.sh
          ./scripts/build_sdk.sh
        shell: bash

      - name: Push SDKs
        run: |
          chmod +ux ./scripts/push_sdk.sh
          ./scripts/push_sdk.sh
        shell: bash